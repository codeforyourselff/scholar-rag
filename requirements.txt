# --- Web framework / server ---
fastapi==0.115.6
uvicorn[standard]==0.34.0        # [standard] adds uvloop + httptools for throughput
 
# --- Config & validation ---
pydantic==2.10.4
pydantic-settings==2.7.0
python-dotenv==1.0.1             # used by pydantic-settings to parse .env
 
# --- Datastores / clients ---
asyncpg==0.30.0                  # Postgres
redis==5.2.1                     # Redis (async via redis.asyncio)
qdrant-client==1.12.1            # Qdrant vector store
 
# --- Models / LLM ---
sentence-transformers==3.3.1     # embeddings; pulls transformers + torch
openai==1.59.9                   # hosted LLM client